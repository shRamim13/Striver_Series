#include <bits/stdc++.h>
using namespace std;

int Reverse(int n)
{
    int rev = 0;
    while (n > 0)
    {
    
    }

    return rev;
}

int main()
{
    while (1)
    {
        int n;
        cin >> n;
        cout << Reverse(n) << endl;
        /* code */
    }
}